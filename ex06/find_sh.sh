#'ls -R' command enables a loop to search each files directory and all of its sub directories , therefore the pipelined 'grep' command specifies the type of files being searched.

ls -R |grep "\.sh$"
